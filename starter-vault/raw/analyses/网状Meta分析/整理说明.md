# 整理说明

这个 raw 项目应被整理为：

```text
wiki/analyses/网状Meta分析/
```

整理步骤：

1. 识别这是一个基于 BUGSnet 的贝叶斯网状 Meta 分析项目。
2. 确认数据是 arm-based format。
3. 提取输入列要求。
4. 提取核心 R 包：`BUGSnet`、`dplyr`、`tidyr`、`ggplot2`、`gridExtra`。
5. 提取代码流程：读数据、拆分结局、`data.prep()`、`nma.model()`、`nma.run()`、`nma.rank()`、`nma.league()`、`nma.forest()`。
6. 写 wiki `README.md`。
7. 写 `manifest.yaml`，让 `search_kb.py` 能检索“网状 Meta 分析 / NMA / BUGSnet / SUCRA”。
8. 更新 `wiki/index.md` 与 `wiki/log.md`。
9. 补充 `wiki/concepts/贝叶斯分析.md`。

